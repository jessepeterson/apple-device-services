{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AccountDetail",
  "description": "The response that contains the details for an account.",
  "x-apple-developer-api-uri": "https://developer.apple.com/documentation/devicemanagement/accountdetail",
  "x-apple-developer-data-uri": "https://developer.apple.com/tutorials/data/documentation/devicemanagement/accountdetail.json",
  "type": "object",
  "properties": {
    "admin_id": {
      "description": "The Apple Account of the person who generated the currently in-use tokens.",
      "type": "string"
    },
    "facilitator_id": {
      "description": "The legacy equivalent to the `admin_id` key. This key is deprecated and may not be returned in future responses.",
      "type": "string"
    },
    "org_address": {
      "description": "The organization address.",
      "type": "string"
    },
    "org_email": {
      "description": "The organization email address.",
      "type": "string"
    },
    "org_id": {
      "description": "The customer ID. This key is available only in protocol version 3 and later.",
      "type": "string"
    },
    "org_id_hash": {
      "description": "The SHA hash of an organization identifier. This helps Mobile Device Management (MDM) server match the hash with the `organizationIdHash` key in the [Client Configuration](https://developer.apple.com/documentation/devicemanagement/client-configuration) API. This key is available only in protocol version 3 and later.",
      "type": "string"
    },
    "org_name": {
      "description": "The organization name.",
      "type": "string"
    },
    "org_phone": {
      "description": "The organization phone.",
      "type": "string"
    },
    "org_type": {
      "description": "The type of organization. Possible values are `edu` or `org`. This key is available only in protocol version 3 and later.",
      "enum": [ "edu", "org", "tvprovider" ]
    },
    "org_version": {
      "description": "Possible values: `v1` or `v2`. `v1` is for Apple Deployment Programs (like Automated Device Enrollment or Volume Purchase Program) organizations and `v2` is for Apple School Manager organizations. Currently `v2` is applicable only to educational organizations. This key is available only in protocol version 3 and later.",
      "enum": [ "v1", "v2" ]
    },
    "server_name": {
      "description": "The name of the MDM server.",
      "type": "string"
    },
    "server_uuid": {
      "description": "The system-generated server identifier.",
      "type": "string"
    },
    "urls": {
      "description": "The list of URLs available in the MDM service. This key is valid in X-Server-Protocol-Version 3 and later.",
      "type": "array",
      "items": {
        "$ref": "Url.json"
      }
    }
  }
}
