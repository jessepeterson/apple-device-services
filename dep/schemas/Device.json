{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Device",
  "description": "A device’s properties and their values.",
  "x-apple-developer-api-uri": "https://developer.apple.com/documentation/devicemanagement/device",
  "x-apple-developer-data-uri": "https://developer.apple.com/tutorials/data/documentation/devicemanagement/device.json",
  "type": "object",
  "required": [ "serial_number", "model" ],
  "properties": {
    "description": {
      "description": "A description of the device.",
      "type": "string"
    },
    "asset_tag": {
      "description": "The device’s asset tag.",
      "type": "string"
    },
    "color": {
      "description": "The color of the device.",
      "type": "string"
    },
    "device_assigned_by": {
      "description": "The email of the person who assigned the device.",
      "type": "string"
    },
    "device_assigned_date": {
      "description": "A time stamp in ISO 8601 format that indicates when the device was assigned to the MDM server.",
      "type": "string",
      "format": "date-time"
    },
    "device_family": {
      "description": "The device’s Apple product family: `iPad`, `iPhone`, `iPod`, `Mac`, `AppleTV`, or `Vision`. This key is valid in X-Server-Protocol-Version 2 and later.",
      "enum": [ "iPad", "iPhone", "iPod", "Mac", "AppleTV", "Vision" ]
    },
    "mdm_migration_deadline": {
      "description": "A time stamp in ISO 8601 format that indicates the MDM migration deadline. This key is valid with X-Server-Protocol-Version 8 and later.",
      "type": "string",
      "format": "date-time"
    },
    "model": {
      "description": "The model name.",
      "type": "string"
    },
    "op_date": {
      "description": "A time stamp in ISO 8601 format that indicates when the device was added, updated, or deleted. If the value of `op_type` is added, this is the same as `device_assigned_date`. This field is only applicable with the [Sync the List of Devices](https://developer.apple.com/documentation/devicemanagement/sync-devices) command.",
      "type": "string",
      "format": "date-time"
    },
    "op_type": {
      "description": "Indicates whether the device was added (assigned to the MDM server), modified, or deleted. Contains one of the following strings: `added`, `modified`, or `deleted`. This field is only applicable with the `sync the list of devices` command.",
      "enum": [ "added", "modified", "deleted" ]
    },
    "os": {
      "description": "The device’s operating system: `iOS`, `iPadOS`, `OSX`, `tvOS`, or `visionOS`.\nThis key is valid in X-Server-Protocol-Version 2 and later.\nWith X-Server-Protocol-Version 7 and later, iPad product os will return `iPadOS`.",
      "enum": [ "iOS", "iPadOS", "OSX", "tvOS", "visionOS" ]
    },
    "profile_assign_time": {
      "description": "A time stamp in ISO 8601 format that indicates when a profile was assigned to the device.",
      "type": "string",
      "format": "date-time"
    },
    "profile_push_time": {
      "description": "A time stamp in ISO 8601 format that indicates when a profile was pushed to the device.",
      "type": "string",
      "format": "date-time"
    },
    "profile_status": {
      "description": "The status of profile installation—either `empty`, `assigned`, `pushed`, or `removed`.",
      "enum": [ "empty", "assigned", "pushed", "removed" ]
    },
    "profile_uuid": {
      "description": "The unique ID of the assigned profile.",
      "type": "string"
    },
    "response_status": {
      "$comment": "Undocumented property returned only for the `Device Details` endpoint.",
      "type": "string"
    },
    "serial_number": {
      "description": "The device’s serial number.",
      "type": "string"
    },
    "bluetooth_mac_address": {
      "description": "The device’s Bluetooth MAC address. This key is valid in X-Server-Protocol-Version 10 and later.",
      "type": "string"
    },
    "eid": {
      "description": "The Embedded Identity Document (EID), sometimes known as the CSN, that uniquely identifies the eSIM chip built into the device. This key is valid in X-Server-Protocol-Version 10 and later.",
      "type": "string"
    },
    "ethernet_mac_address": {
      "description": "The device’s Ethernet MAC address. This key is valid in X-Server-Protocol-Version 10 and later.",
      "type": "string"
    },
    "imei": {
      "description": "An array of strings containing the International Mobile Equipment Identity (IMEI) numbers that identify the device. This key is valid in X-Server-Protocol-Version 10 and later.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "is_replacement_device": {
      "description": "If `true`, the device is a replacement for another device. Use [Get Replacement Details](https://developer.apple.com/documentation/devicemanagement/get-replacement-details) to retrieve information about the device it replaced. This key is valid in X-Server-Protocol-Version 10 and later.",
      "type": "boolean"
    },
    "meid": {
      "description": "An array of strings containing the Mobile Equipment Identifier (MEID) numbers that identify CDMA-based mobile devices. This key is valid in X-Server-Protocol-Version 10 and later.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "released_by_replacement": {
      "description": "If `true`, the device was released from the MDM server because it was replaced by another device. This key is only present with [Sync the List of Devices](https://developer.apple.com/documentation/devicemanagement/sync-devices) when `op_type` is `deleted`. This key is valid in X-Server-Protocol-Version 10 and later.",
      "type": "boolean"
    },
    "wifi_mac_address": {
      "description": "The device’s Wi-Fi MAC address. This key is valid in X-Server-Protocol-Version 10 and later.",
      "type": "string"
    }
  }
}
