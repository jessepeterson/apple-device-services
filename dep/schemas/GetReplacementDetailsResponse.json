{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GetReplacementDetailsResponse",
  "description": "Information about a replacement device, including the original device it replaces and the date the replacement occurred.",
  "x-apple-developer-api-uri": "https://developer.apple.com/documentation/devicemanagement/getreplacementdetailsresponse",
  "x-apple-developer-data-uri": "https://developer.apple.com/tutorials/data/documentation/devicemanagement/getreplacementdetailsresponse.json",
  "type": "object",
  "required": [
    "serial_number",
    "original_device_serial_number",
    "replacement_date"
  ],
  "properties": {
    "serial_number": {
      "description": "The serial number of the replacement device.",
      "type": "string"
    },
    "original_device_serial_number": {
      "description": "The serial number of the original device that this device replaces.",
      "type": "string"
    },
    "replacement_date": {
      "description": "The date when the device replacement occurred, in ISO 8601 format with day granularity in UTC (for example, `2025-01-15`).",
      "type": "string",
      "format": "date"
    }
  }
}
